# =============================================================================
# II-Agent Environment Variables
# =============================================================================
# Copy this file to .env.ii-agent and fill in all values.
#
#   cp infra/.env.ii-agent.example infra/.env.ii-agent
#
# Then deploy with:
#   docker compose -f infra/docker-compose.prod.yml \
#     -f infra/docker-compose.ii-agent.yaml \
#     --env-file infra/.env.ii-agent up -d
#
# WARNING: Never commit .env.ii-agent to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Required: LLM API Key (ii-agent default uses Anthropic)
# -----------------------------------------------------------------------------
# Get key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# OpenRouter — alternative/fallback LLM access (200+ models)
# Get key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# -----------------------------------------------------------------------------
# PostgreSQL Database (ii-agent task persistence)
# -----------------------------------------------------------------------------
# Generate a strong password: openssl rand -base64 24
II_AGENT_DB_USER=iiagent
II_AGENT_DB_PASSWORD=
II_AGENT_DB_NAME=iiagentdev

# These are auto-derived from the above — no need to set manually.
# DATABASE_URL=postgresql://iiagent:<password>@ii-agent-postgres:5432/iiagentdev
# SANDBOX_DATABASE_URL=postgresql://iiagent:<password>@ii-agent-postgres:5432/iiagentdev_sandbox

# -----------------------------------------------------------------------------
# Optional: Code Sandbox (cloud-based isolated execution)
# -----------------------------------------------------------------------------
# E2B — remote sandbox for untrusted code execution
# Get key at https://e2b.dev/dashboard
E2B_API_KEY=

# -----------------------------------------------------------------------------
# Optional: Search APIs (used by ii-agent tool server)
# -----------------------------------------------------------------------------
# These are shared with the main AIMS stack — set in .env.production
# Only set here if ii-agent needs different keys.
# BRAVE_API_KEY=
# SERPER_API_KEY=
# TAVILY_API_KEY=

# -----------------------------------------------------------------------------
# Connection (UEF Gateway → ii-agent)
# -----------------------------------------------------------------------------
# These defaults work for same-VPS deployment via docker-compose overlay.
# Only change if ii-agent runs on a separate host.
# II_AGENT_HTTP_URL=http://ii-agent:8000
# II_AGENT_WS_URL=ws://ii-agent:8000
# II_AGENT_SERVICE_TOKEN=aims-service-token
