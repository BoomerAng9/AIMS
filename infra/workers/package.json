{
  "name": "aims-workers",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers for AIMS edge compute — R2 signed URLs, agent gateway, caching",
  "scripts": {
    "dev:r2": "cd r2-signer && npx wrangler dev",
    "dev:gateway": "cd agent-gateway && npx wrangler dev",
    "deploy:r2": "cd r2-signer && npx wrangler deploy",
    "deploy:gateway": "cd agent-gateway && npx wrangler deploy",
    "deploy:all": "npm run deploy:r2 && npm run deploy:gateway",
    "types": "cd r2-signer && npx wrangler types && cd ../agent-gateway && npx wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.99.0"
  }
}
